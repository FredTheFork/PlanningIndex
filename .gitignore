# Next.js build output — never commit this
.next/
out/

# Dependencies — Vercel installs these itself
node_modules/

# Environment variables — never commit secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel internals
.vercel/

# OS junk
.DS_Store
Thumbs.db

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
